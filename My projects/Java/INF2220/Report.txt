The complexity of implementation is O(E+V) which in my case E is number of tasks and V is number of outEdges.In checking cyclic it is O(E+V) because it performs DFS depth first search and each node is visited once. Each and every method goes through a certain numbers of verticies and edgdes so the complexity is O(E+V).